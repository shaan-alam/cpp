#include <iostream>
using namespace std;

int main() {
  int a = 10, b = 20, c =30;

  int max = a;

  if (max > b) {
    max = b;
  } 
  if (max > c) {
    max = c;
  }
  
  {
    (
      [
    )
  }

  cout << "Maximum number: " << max << endl;
}